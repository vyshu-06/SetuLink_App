import 'package:flutter/material.dart';

class DisputeFormScreen extends StatelessWidget {
  const DisputeFormScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Dispute Form')),
      body: const Center(
        child: Text('Dispute Form Screen - Under Development'),
      ),
    );
  }
}

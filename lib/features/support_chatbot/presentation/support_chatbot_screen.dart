import 'package:flutter/material.dart';

class SupportChatbotScreen extends StatelessWidget {
  const SupportChatbotScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Support Chatbot')),
      body: const Center(
        child: Text('Support Chatbot Screen - Under Development'),
      ),
    );
  }
}
